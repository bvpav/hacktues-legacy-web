<% provide(:title, 'Нов отбор') %>
<h1>Нов отбор</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@team) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Създай отбор", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
